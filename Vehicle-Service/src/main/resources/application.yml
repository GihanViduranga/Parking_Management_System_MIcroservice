server:
  port: 8083
  servlet:
    context-path: /vehicle-service

spring:
  application:
    name: vehicle-service
  cloud:
    config:
      enabled: false

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8081/eureka