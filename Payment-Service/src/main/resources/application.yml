server:
  port: 8085
  servlet:
    context-path: /payment-service

spring:
  application:
    name: payment-service
  cloud:
    config:
      enabled: false

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8081/eureka