server:
  port: 8082
  servlet:
    context-path: /user-auth-service

spring:
  application:
    name: user-auth-service
  cloud:
    config:
      enabled: false

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8081/eureka